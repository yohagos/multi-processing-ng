import { AbbreviateTextsPipe } from './abbreviate-texts-pipe';

describe('AbbreviateTextsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AbbreviateTextsPipe();
    expect(pipe).toBeTruthy();
  });
});
