import { NamesFormatterPipe } from './names-formatter-pipe';

describe('NamesFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new NamesFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
